---
page_ref: "@ARK_PROJECT__VARIANT@/clandro/clandro-exec-package/releases/2/v2.0.0.html"
---

# clandro-exec-package v2.0.0 - 2025-03-21

## Changelog

**Commit history:** [`v1.0...v2.0.0`](https://github.com/cl-andro/clandro-exec-package/compare/v1.0...v2.0.0)

&nbsp;



### Added

- The `libclandro-exec_nos_c_tre` c library has been added to handle all the `LD_PRELOAD` intercepts implementation, and other functionality required for `clandro-exec` like the environment/config. This can be used statically or dynamically for `clandro-exec` executables/libraries and also for other packages. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))

- The `libclandro-exec-direct-ld-preload.so` `$LD_PRELOAD` library variant has been added. It statically depends on `libclandro-exec_nos_c_tre` library and primarily includes `app/clandro-exec-direct-ld-preload/src/clandro/api/clandro_exec/ld_preload/direct/ClandroExecDirectLDPreloadEntryPoint.c`, which now exclusively only defines the functions intercepted by `libclandro-exec-direct-ld-preload.so` and directs them to their intercepts in respective source files of `libclandro-exec_nos_c_tre` library. The `libclandro-exec-direct-ld-preload.so` is used as primary `$LD_PRELOAD` library variant by copying it to `$CLANDRO__PREFIX/usr/lib/libclandro-exec-ld-preload.so` and this path is exported in `$LD_PRELOAD` by the `login` script. For backward compatibility, `libclandro-exec.so` is also created as a copy of `libclandro-exec-ld-preload.so`  so that older clients do not break which have exported path to `libclandro-exec.so` in `$LD_PRELOAD`. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))

- The intercepts implementation of `execve` done by `src/clandro-exec.c` has been moved to `lib/clandro-exec_nos_c_tre/src/clandro/api/clandro_exec/ld_preload/direct/exec/ExecIntercept.c`. The `src/exec-variants.c` has been moved to `lib/clandro-exec_nos_c_tre/src/clandro/api/clandro_exec/ld_preload/direct/exec/ExecVariantsIntercept.c` which handles intercepts of the entire `exec()` family of functions except `execve()`. The intercept implementations are now called from `app/clandro-exec-direct-ld-preload/src/clandro/api/clandro_exec/ld_preload/direct/ClandroExecDirectLDPreloadEntryPoint.c`. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))

&nbsp;

- Intercept the entire `exec()` family of functions, which is required for Android `14`. Closes clandro/clandro-packages#18537, clandro/clandro-app#3758. ([`5ea25ee3`](https://github.com/cl-andro/clandro-exec-package/commit/5ea25ee3), [`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))
- Added the `string` environment variable `CLANDRO_EXEC__EXECVE_CALL__INTERCEPT` for whether `execve` would be intercepted for shebang fix or `system_linker_exec`. If set to `enable`, then `execve()` intercept will be enabled. If set to `disable`, then `execve()` intercept will be disabled. The default value is `enable`. The other wrapper functions in the `exec()` family of functions declared in `unistd.h` are always intercepted to solve some other issues on older Android versions, check [`libc/bionic/exec.cpp`](https://cs.android.com/android/platform/superproject/+/android-14.0.0_r1:bionic/libc/bionic/exec.cpp) git history. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))

&nbsp;

- Add support to execute ELF files by passing them to `/system/bin/linker*` to bypass android app data file exec restriction if using `targetSdkVersion` `>= 28` on Android `>= 10`. Check [technical](https://github.com/cl-andro/clandro-exec-package/blob/master/site/pages/en/projects/docs/technical/index.md) and [system linker exec](https://github.com/agnostic-apollo/Android-Docs/blob/master/site/pages/en/projects/docs/apps/processes/app-data-file-execute-restrictions.md#system-linker-exec) docs for info on how `system_linker_exec` works and why it is needed. ([`db738a11`](https://github.com/cl-andro/clandro-exec-package/commit/db738a11))
- The `libclandro-exec-linker-ld-preload.so` `$LD_PRELOAD` library variant has been added and is meant to intercept additional functions to solve other issues specific to `system_linker_exec` execution. The `libclandro-exec-direct-ld-preload.so` variant needs to support `system_linker_exec` as well as during package updates, before the `linker` variant is set as primary variant, the `direct` variant will get used for certain commands as it gets installed as the primary variant by default, and commands will fail if `system_linker_exec` is required to bypass execution restrictions. The primary `$LD_PRELOAD` library variant to be used is set by copying it to `$CLANDRO__PREFIX/usr/lib/libclandro-exec-ld-preload.so` and this path is exported in `$LD_PRELOAD` by `login` script. This is done by the `postinst` script run during package installation, which runs `clandro-exec-ld-preload-lib setup` to set the correct variant as per the execution type required for the Clandro environment of the host device by running `clandro-exec-system-linker-exec is-enabled` to check if `system_linker_exec` is to be enabled. ([`db738a11`](https://github.com/cl-andro/clandro-exec-package/commit/db738a11))
- Since when executing with linker, the `/proc/self/exe` will be set to linker path, export `CLANDRO_EXEC__PROC_SELF_EXE` environment variable with actual path to executable being executed so that packages can be patched to read it instead. Additional hocking will need to be done for programs that read `/proc/<pid>/exe`. ([`5ea25ee3`](https://github.com/cl-andro/clandro-exec-package/commit/5ea25ee3), [`db738a11`](https://github.com/cl-andro/clandro-exec-package/commit/db738a11))
- Added the `string` `CLANDRO_EXEC__SYSTEM_LINKER_EXEC__MODE` environment variable for whether to use `system_linker_exec` if `CLANDRO_EXEC__EXECVE_CALL__INTERCEPT` is enabled. If set to `disable`, `system_linker_exec` will be disabled. If set to `enable`, then `system_linker_exec` will be enabled but only if required. If set to `force`, then `system_linker_exec` will be force enabled even if not required and is supported. The default value is `enable`. Check `isSystemLinkerExecEnabled()` function ([1](https://github.com/cl-andro/clandro-exec-package/blob/v2.0.0/lib/clandro-exec_nos_c_tre/include/clandro/clandro_exec__nos__c/v1/clandro/api/clandro_exec/ld_preload/ClandroExecLDPreload.h#L20), [2](https://github.com/cl-andro/clandro-exec-package/blob/v2.0.0/lib/clandro-exec_nos_c_tre/src/clandro/api/clandro_exec/ld_preload/ClandroExecLDPreload.c#L31)) and `shouldEnableSystemLinkerExecForFile()` function ([1](https://github.com/cl-andro/clandro-exec-package/blob/v2.0.0/lib/clandro-exec_nos_c_tre/include/clandro/clandro_exec__nos__c/v1/clandro/api/clandro_exec/ld_preload/ClandroExecLDPreload.h#L55), [2](https://github.com/cl-andro/clandro-exec-package/blob/v2.0.0/lib/clandro-exec_nos_c_tre/src/clandro/api/clandro_exec/ld_preload/ClandroExecLDPreload.c#L2137)) in `ClandroExecLDPreload.h` and implemented by `ClandroExecLDPreload.c` for more info and how its handled. The `system_linker_exec` will now engage for executable or interpreter paths that are under `CLANDRO_APP__DATA_DIR` or `CLANDRO_APP__LEGACY_DATA_DIR` instead of `CLANDRO__ROOTFS` (`CLANDRO_BASE_DIR`). ([`db738a11`](https://github.com/cl-andro/clandro-exec-package/commit/db738a11))

&nbsp;

- Added logger framework with multiple log levels with log entries for all the important variable states to track logic. The singular `CLANDRO_EXEC_DEBUG` environment variable has been removed. The `int` `CLANDRO_EXEC__LOG_LEVEL` environment variable controls the log level based on `(OFF=0, NORMAL=1, DEBUG=2, VERBOSE=3, VVERBOSE=4 and VVVERBOSE=5)`. The default value is `1`. Normally, `clandro-exec` does not log anything at default log level `1` (`NORMAL`) for intercepts even and will require setting log level to `>= 2` (`DEBUG`) to see log messages. To enable `VVERBOSE` logging for a command, you can run something like `CLANDRO_EXEC__LOG_LEVEL=4 id -u`. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))
- Added `CLANDRO_EXEC_PKG__VERSION` `Makefile` parameter that gets logged on intercept for `clandro-exec` package version currently installed. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))

&nbsp;

- Added testing framework via `app/main/tests/clandro-exec-tests.in` and `lib/clandro-exec_nos_c_tre/tests/libclandro-exec_nos_c_tre_tests.in` that calls `clandro-exec/lib/clandro-exec_nos_c_tre/tests/src/libclandro-exec_nos_c_tre_unit-binary-tests.c` to run unit tests, and `lib/clandro-exec_nos_c_tre/tests/src/libclandro-exec_nos_c_tre_runtime-binary-tests.c` and `lib/clandro-exec_nos_c_tre/tests/scripts/libclandro-exec_nos_c_tre_runtime-script-tests.in` for runtime tests. Old tests files in random places have been removed. The entire `exec()` family of functions is also tested by `lib/clandro-exec_nos_c_tre/tests/src/clandro/api/clandro_exec/ld_preload/direct/exec/ExecIntercept_RuntimeBinaryTests.c`. Docs will be added in a later commit.Tests can be run with `"${CLANDRO__PREFIX:-$PREFIX}/libexec/installed-tests/clandro-exec/app/main/clandro-exec-tests -vv all"`. ([`ad67e020`](https://github.com/cl-andro/clandro-exec-package/commit/ad67e020))

&nbsp;

- Add site, docs and old changelog files under `MIT` license. ([`2fe47750`](https://github.com/cl-andro/clandro-exec-package/commit/2fe47750), [`21e6e634`](https://github.com/cl-andro/clandro-exec-package/commit/21e6e634))

## &nbsp;

&nbsp;



### Changed

- Use `CLANDRO__PREFIX` environment variable to generate Clandro bin path to replace `/bin` and `/usr/bin` path in shebang of scripts instead of using hardcoded `CLANDRO_BASE_DIR` build variable. If environment variable is not set or is invalid as per `CLANDRO__ROOTFS_DIR___MAX_LEN`, then we use the default Clandro prefix for which package was compiled for as long as its executable and readable to ensure clandro-exec was not compiled for a different package. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))
- Increase buffer size for executable file shebang header from `256` to `340` defined by `CLANDRO__FILE_HEADER__BUFFER_SIZE` as per clandro path limits, check comment in `ExecIntercept.h` file and [Clandro File Path Limits](https://github.com/cl-andro/clandro-packages/wiki/Clandro-file-system-layout#file-path-limits) docs. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))
- Decrease max length of valid `CLANDRO__ROOTFS` from `200` to `86` defined by `CLANDRO__ROOTFS_DIR___MAX_LEN`, check `ClandroFile.h` file and [Clandro File Path Limits](https://github.com/cl-andro/clandro-packages/wiki/Clandro-file-system-layout#file-path-limits) docs. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))
- Move `clandro-exec` Apache 2.0 license file content from `LICENSE` to `licenses/clandro__clandro-exec__Apache-2.0.md` file and use `debian/copyright` format in `LICENSE` file. ([`7ccbc61e`](https://github.com/cl-andro/clandro-exec-package/commit/7ccbc61e))

## &nbsp;

&nbsp;



### Fixed

- Fix relative paths for interpreter path by absolutizing it. Previously, only prefix was being replaced. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))
- Fix `argv[0]` for executing shell scripts where it should be set to the original interpreter set in the file as is instead of the `argv[0]` to `execve()` being intercepted for the executable. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))
- Fix `fexecve()` where executable path would be `/proc/self/fd/<n>` and checking if its under Clandro app data directory directory would give wrong results. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))
- Fix checking if executable is under Clandro directories. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))
- Fix hardcoded `com.clandro` values being used, all constants are replaced during building including the root scope of environment variables that are read and as per `CLANDRO_ENV__S_ROOT` defined in `properties.sh` of `clandro-pacakges` as `CLANDRO_`. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))
- Abort `execve()` with a debug error if `argv[0]` length is `>= 128` on Android `< 6` instead of letting process fail post `execve()` without an error. ([`e726fba2`](https://github.com/cl-andro/clandro-exec-package/commit/e726fba2))
- Fix issues where `errno` may already be set when `execve` is entered, check comment in `init()` function of `ClandroExecProcess.c` where it is set to `0`. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))
- The `clandro-exec-package.json` will now have correct version and be consistent with `build.sh`, and also include tests files, which wasn't being done before. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))
- The `clandro-exec-package.json` previously had hardcoded `aarch64` as architecture, now we find and replace it for the compiler based on which predefined architecture macro is defined. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))

## &nbsp;

&nbsp;



### Removed

- Removed the singular `CLANDRO_EXEC_OPTOUT` environment variable. Opt outs should be confined to specific intercepts and logics. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))
- The singular `CLANDRO_EXEC_DEBUG` environment variable has been removed. ([`1fac1073`](https://github.com/cl-andro/clandro-exec-package/commit/1fac1073))

---

&nbsp;





## Notes

- Usage docs are available [here](https://github.com/cl-andro/clandro-exec-package/blob/master/site/pages/en/projects/docs/usage/index.md). You can run commands with `CLANDRO_EXEC__LOG_LEVEL=4 id -u` after installing package for debugging. To run on normal clandro with `targetSdkVersion` `<= 28`, run `CLANDRO_EXEC__SYSTEM_LINKER_EXEC__MODE=force CLANDRO_EXEC__LOG_LEVEL=4 id -u` instead. **Check [`CLANDRO_EXEC__LOG_LEVEL`](https://github.com/cl-andro/clandro-exec-package/blob/master/site/pages/en/projects/docs/usage/index.md#clandro_exec__log_level), [`CLANDRO_EXEC__EXECVE_CALL__INTERCEPT`](https://github.com/cl-andro/clandro-exec-package/blob/master/site/pages/en/projects/docs/usage/index.md#clandro_exec__execve_call__intercept), [`CLANDRO_EXEC__SYSTEM_LINKER_EXEC__MODE`](https://github.com/cl-andro/clandro-exec-package/blob/master/site/pages/en/projects/docs/usage/index.md#clandro_exec__system_linker_exec__mode) variable docs for more info on how to control behaviour of `clandro-exec`.**
- Testing docs are available [here](https://github.com/cl-andro/clandro-exec-package/blob/master/site/pages/en/projects/docs/developer/test/index.md). Run tests with `CLANDRO_ROOTFS__PACKAGE_MANAGER=apt "${CLANDRO__PREFIX:-$PREFIX}/libexec/installed-tests/clandro-exec/app/main/clandro-exec-tests" -vv all`. Exporting `CLANDRO_ROOTFS__PACKAGE_MANAGER` variables is necessary for running tests on older Clandro app versions without scoped environment variable changes for package manager tests to run.
- Build docs are available [here](https://github.com/cl-andro/clandro-exec-package/blob/master/site/pages/en/projects/docs/developer/build/index.md).

---

&nbsp;
